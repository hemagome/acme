# .lefthook.yml
pre-commit:
  parallel: true
  commands:
    fmt:
      run: bun run format
      verbose: true
    lint:
      run: bun run lint
      verbose: true
    test:
      run: bun run test
      verbose: true

pre-push:
  parallel: false
  commands:
    build:
      run: bun run build
      verbose: true
    test:
      run: bun run test
      verbose: true
